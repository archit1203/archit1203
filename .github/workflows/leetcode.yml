name: Update LeetCode Stats

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: joshcai/leetcode-stats-action@v1
        with:
          username: archit1203
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
